pandas>=2.0
matplotlib>=3.7
plotly>=5.18
numpy>=1.24
datasets>=2.14              # HuggingFace datasets library
pyarrow>=12.0               # parquet read/write
huggingface_hub>=0.19
rich>=13.0                  # optional: pretty console tables
# Phase 3 — Router
sentence-transformers>=2.7  # prompt embeddings (all-MiniLM-L6-v2)
scikit-learn>=1.3           # LogisticRegression, cross-validation
joblib>=1.3                 # model serialisation
# Phase 4 — Improved embeddings (code-specific)
transformers>=4.38          # CodeBERT, CodeT5 embeddings
torch>=2.0                  # required by transformers
